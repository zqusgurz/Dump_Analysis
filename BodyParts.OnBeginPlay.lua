return function (self) 
--local cat = self.CategoryToBodyPart
--cat[MapleAvatarItemCategory.Cap] = self.Cap
--cat[MapleAvatarItemCategory.FaceAcc] = self.FaceAcc
--cat[MapleAvatarItemCategory.EyeAcc] = self.EyeAcc
--cat[MapleAvatarItemCategory.EarAcc] = self.EarAcc
--cat[MapleAvatarItemCategory.Clothes] = self.Clothes
--cat[MapleAvatarItemCategory.Pants] = self.Pants
--cat[MapleAvatarItemCategory.Shoes] = self.Shoes
--cat[MapleAvatarItemCategory.Gloves] = self.Gloves
--cat[MapleAvatarItemCategory.Cape] = self.Cape
--cat[MapleAvatarItemCategory.Shield] = self.Shield
--cat[MapleAvatarItemCategory.OneHandedWeapon] = self.Weapon
--cat[MapleAvatarItemCategory.TwoHandedWeapon] = self.Weapon
end